import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  channels: {
    flexDirection: "row",
    flexWrap: "wrap",
    justifyContent: "center",
    marginTop: 100,
    paddingLeft: 20,
  },
  channelButton: {
    justifyContent: "center",
    alignItems: "center",
  },
  fullPaddingChannelButton: {
    paddingLeft: 10,
    paddingRight: 10,
  },
});
