import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    position: 'absolute',
    top: 0,
    right: 0,
    zIndex: 100,
  },
  pressable: {
    paddingTop: 16,
    paddingRight: 24,
    paddingLeft: 19,
    paddingBottom: 8,
  },
});
